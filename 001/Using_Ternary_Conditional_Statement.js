/*Condition ? statement-if-true : statement-if-false;         */
function testCoding(value){
    return value > 10 ? "Absolute Value" : value < 5 ? "Perfect Match" : "Nearly Absolute";
}
testCoding(19);
